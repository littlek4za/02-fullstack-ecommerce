import { CustomCOFormValidator } from './custom-coform-validator';

describe('CustomCOFormValidator', () => {
  it('should create an instance', () => {
    expect(new CustomCOFormValidator()).toBeTruthy();
  });
});
